# Local development. On Vercel replace this with the real HTTPS Render URL.
# Set for Production, Preview, and Development, then redeploy.
# NEXT_PUBLIC values are public. Never put provider keys here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
